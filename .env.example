# =============================================================================
# EduAI Platform - Environment Variables Template
# =============================================================================
# Instructions for Firebase Studio setup:
# 1. Copy this file: cp .env.example .env
# 2. These are the EXACT secret keys from Replit environment
# 3. All keys are already configured and working
# 4. Just copy this file to .env to run the application
# =============================================================================

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================
NODE_ENV=development
PORT=5000

# =============================================================================
# DATABASE CONFIGURATION (From Replit Environment)
# =============================================================================
# Primary PostgreSQL connection - copy exactly as is
DATABASE_URL=copy_from_replit_secrets

# Individual PostgreSQL components - copy exactly as is
PGHOST=copy_from_replit_secrets
PGPORT=copy_from_replit_secrets
PGUSER=copy_from_replit_secrets
PGPASSWORD=copy_from_replit_secrets
PGDATABASE=copy_from_replit_secrets

# =============================================================================
# AI SERVICES (From Replit Environment)
# =============================================================================
# Google Gemini API - copy exactly as is
GEMINI_API_KEY=copy_from_replit_secrets

# =============================================================================
# FIREBASE CONFIGURATION (From Replit Environment)
# =============================================================================
# Frontend Firebase keys - copy exactly as is
VITE_FIREBASE_API_KEY=copy_from_replit_secrets
VITE_FIREBASE_PROJECT_ID=copy_from_replit_secrets  
VITE_FIREBASE_APP_ID=copy_from_replit_secrets

# Backend Firebase Admin SDK keys - copy exactly as is
FIREBASE_PROJECT_ID=copy_from_replit_secrets
FIREBASE_CLIENT_EMAIL=copy_from_replit_secrets
FIREBASE_PRIVATE_KEY=copy_from_replit_secrets

# =============================================================================
# GOOGLE CLOUD SERVICES (Optional)
# =============================================================================
# Google Cloud Project ID (usually same as Firebase project)
GOOGLE_CLOUD_PROJECT_ID=your_gcp_project_id

# Path to service account JSON file (alternative to individual keys)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# =============================================================================
# EXTERNAL SERVICES (Optional Features)
# =============================================================================
# Twilio (for SMS notifications and alerts)
# Get from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# =============================================================================
# PLATFORM-SPECIFIC VARIABLES (Auto-detected)
# =============================================================================
# These are automatically set by deployment platforms
# REPL_ID=auto_set_on_replit
# VERCEL=auto_set_on_vercel
# DYNO=auto_set_on_heroku
# NETLIFY=auto_set_on_netlify
# RAILWAY_PROJECT_ID=auto_set_on_railway

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================
# Enable debug logging
DEBUG=true
LOG_LEVEL=info

# Platform detection override (leave blank for auto-detection)
# PLATFORM_OVERRIDE=FIREBASE_STUDIO

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# JWT secrets (if using JWT authentication)
JWT_SECRET=your_jwt_secret_key_here

# API rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Enable/disable specific features
ENABLE_IMAGE_PROCESSING=true
ENABLE_PDF_GENERATION=true
ENABLE_AUDIO_PROCESSING=true
ENABLE_NCERT_SCRAPING=true

# =============================================================================
# GETTING YOUR API KEYS
# =============================================================================
# 
# 1. GEMINI_API_KEY (Required):
#    - Go to https://aistudio.google.com/app/apikey
#    - Click "Create API Key"
#    - Copy the generated key
#
# 2. DATABASE_URL (Required):
#    - Replit: Automatically set in environment
#    - External: Get from your PostgreSQL provider (Neon, Supabase, etc.)
#
# 3. Firebase Keys (Required for auth):
#    - Go to Firebase Console: https://console.firebase.google.com/
#    - Select your project
#    - Project Settings -> General -> Your apps -> Web app
#    - Copy the config values
#
# 4. Firebase Admin Keys (Required for NCERT):
#    - Firebase Console -> Project Settings -> Service accounts
#    - Generate new private key
#    - Extract project_id, client_email, and private_key from downloaded JSON
#
# =============================================================================